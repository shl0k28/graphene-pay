import React from 'react'
import { useContractKit } from '@celo-tools/use-contractkit'

const CeloPay: React.FC = () => {

    const { address, connect} = useContractKit()
    return(
        <div>

        </div>
    )
}

export default CeloPay